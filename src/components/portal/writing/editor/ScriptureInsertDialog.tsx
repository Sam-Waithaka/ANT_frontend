import { ChevronDown, LoaderCircle, ScrollText, X } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import BibleVersionPickerList from '../../../../components/scripture/BibleVersionPickerList';
import ScriptureReferencePickerGroup, { type ReferencePickerMenu } from '../../../../components/scripture/ScriptureReferencePickerGroup';
import { getBibleBooks, getBibleChapters, getBibleVerses, getBibleVersions } from '../../../../services/scriptureApi';
import type { BibleBook, BibleChapter, BibleVerse, BibleVersion } from '../../../../types/scripture';
import type { ScriptureData, ScriptureDisplay } from './nodes/scriptureTypes';
import { formatScriptureVerseSelection } from './scriptureSelection';

type ScriptureInsertDialogProps = { darkMode: boolean; initialData?: ScriptureData; onClose: () => void; onInsert: (data: ScriptureData) => void; };
type Mode = 'library' | 'manual';
type PickerMenu = 'version' | ReferencePickerMenu;

const ScriptureInsertDialog = ({ darkMode, initialData, onClose, onInsert }: ScriptureInsertDialogProps) => {
  const [mode, setMode] = useState<Mode>(initialData ? 'manual' : 'library');
  const [display, setDisplay] = useState<ScriptureDisplay>(initialData?.display || 'block');
  const [versions, setVersions] = useState<BibleVersion[]>([]);
  const [books, setBooks] = useState<BibleBook[]>([]);
  const [chapters, setChapters] = useState<BibleChapter[]>([]);
  const [verses, setVerses] = useState<BibleVerse[]>([]);
  const [versionId, setVersionId] = useState('');
  const [bookId, setBookId] = useState('');
  const [chapterId, setChapterId] = useState('');
  const [selectedNumbers, setSelectedNumbers] = useState<number[]>([]);
  const [manualReference, setManualReference] = useState(initialData?.reference || '');
  const [manualVersion, setManualVersion] = useState(initialData?.version || '');
  const [manualText, setManualText] = useState(initialData?.text || '');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');
  const [openPicker, setOpenPicker] = useState<PickerMenu>(null);

  useEffect(() => { let cancelled = false; setLoading(true); getBibleVersions().then((next) => { if (!cancelled) { setVersions(next); setVersionId(next[0]?.id || ''); } }).catch(() => !cancelled && setError('Bible versions could not be loaded. You can still use Manual Entry.')).finally(() => !cancelled && setLoading(false)); return () => { cancelled = true; }; }, []);
  useEffect(() => { if (!versionId) return; let cancelled = false; setBooks([]); setChapters([]); setVerses([]); setBookId(''); setChapterId(''); setSelectedNumbers([]); setLoading(true); getBibleBooks(versionId).then((next) => { if (!cancelled) { setBooks(next); setBookId(next[0]?.id || ''); } }).catch(() => !cancelled && setError('Books could not be loaded for this version.')).finally(() => !cancelled && setLoading(false)); return () => { cancelled = true; }; }, [versionId]);
  useEffect(() => { if (!versionId || !bookId) return; let cancelled = false; setChapters([]); setVerses([]); setChapterId(''); setSelectedNumbers([]); setLoading(true); getBibleChapters(versionId, bookId).then((next) => { if (!cancelled) { setChapters(next); setChapterId(next[0]?.id || ''); } }).catch(() => !cancelled && setError('Chapters could not be loaded.')).finally(() => !cancelled && setLoading(false)); return () => { cancelled = true; }; }, [bookId, versionId]);
  const selectedChapter = chapters.find((item) => item.id === chapterId || String(item.number) === chapterId);
  useEffect(() => { if (!versionId || !bookId || !selectedChapter) return; let cancelled = false; setVerses([]); setSelectedNumbers([]); setLoading(true); getBibleVerses(versionId, bookId, selectedChapter.id, selectedChapter.number).then((next) => { if (!cancelled) { const visible = next.filter((verse) => verse.number > 0 && verse.text); setVerses(visible); setSelectedNumbers(visible[0]?.number ? [visible[0].number] : []); } }).catch(() => !cancelled && setError('Verses could not be loaded.')).finally(() => !cancelled && setLoading(false)); return () => { cancelled = true; }; }, [bookId, selectedChapter, versionId]);

  const selectedVersion = versions.find((item) => item.id === versionId);
  const selectedBook = books.find((item) => item.id === bookId);
  const selectedVerses = useMemo(() => verses.filter((verse) => selectedNumbers.includes(verse.number)), [selectedNumbers, verses]);
  const selectedReference = selectedNumbers.length && selectedChapter ? `${selectedBook?.name || bookId} ${selectedChapter.number}:${formatScriptureVerseSelection(selectedNumbers)}` : '';
  const toggleVerse = (number: number) => setSelectedNumbers((current) => current.includes(number) ? current.filter((item) => item !== number) : [...current, number].sort((left, right) => left - right));
  const inputClass = darkMode ? 'w-full rounded-xl border border-white/10 bg-[#080808] px-3 py-2 text-sm text-stone-100 outline-none focus:ring-2 focus:ring-red-800/30' : 'w-full rounded-xl border border-black/10 bg-[#fffaf0] px-3 py-2 text-sm text-zinc-950 outline-none focus:ring-2 focus:ring-red-800/30';
  const surfaceClass = darkMode ? 'border-white/10 bg-[#171717] text-stone-100' : 'border-black/10 bg-white text-zinc-950';
  const menuClass = darkMode ? 'z-20 rounded-2xl border border-white/10 bg-zinc-950 p-3 shadow-2xl' : 'z-20 rounded-2xl border border-black/10 bg-white p-3 shadow-2xl';

  const insert = () => {
    if (mode === 'manual') { if (!manualReference.trim() || !manualText.trim()) { setError('Reference and Scripture text are required.'); return; } onInsert({ display, reference: manualReference.trim(), source: 'manual', text: manualText.trim(), version: manualVersion.trim() }); return; }
    if (!selectedReference || selectedVerses.length === 0 || !selectedChapter) { setError('Choose at least one verse.'); return; }
    onInsert({ book_osis: selectedBook?.osis_id || selectedBook?.id || bookId, bookLabel: selectedBook?.name || bookId, chapter_start: selectedChapter.number, display, display_text: selectedReference, reference: selectedReference, source: 'api', sourceId: `${versionId}:${bookId}:${selectedChapter.number}:${selectedNumbers.join(',')}`, text: selectedVerses.map((verse) => verse.text).join('\n'), verse_end: selectedNumbers.length > 1 ? selectedNumbers[selectedNumbers.length - 1] : null, verse_start: selectedNumbers[0], version: selectedVersion?.abbreviation || selectedVersion?.name || versionId || 'BSB', verses: selectedVerses.map((verse) => ({ number: verse.number, text: verse.text })) });
  };

  return <div aria-modal="true" className="fixed inset-0 z-50 grid place-items-end bg-black/45 p-4 sm:place-items-center" role="dialog"><section aria-labelledby="scripture-dialog-title" className={'max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-2xl border p-5 shadow-2xl sm:p-6 ' + surfaceClass}><header className="flex items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-red-800 dark:text-red-200">Insert Scripture</p><h2 className="mt-2 font-serif text-2xl" id="scripture-dialog-title">Set apart the passage</h2></div><button aria-label="Close Scripture dialog" className="grid size-10 place-items-center rounded-full border border-black/10 hover:bg-black/5 dark:border-white/10 dark:hover:bg-white/10" onClick={onClose} type="button"><X size={18} /></button></header><div className="mt-5 flex gap-2 border-b border-black/10 pb-3 dark:border-white/10"><button className={mode === 'library' ? 'rounded-full bg-red-800 px-4 py-2 text-sm font-bold text-white' : 'rounded-full px-4 py-2 text-sm font-bold'} onClick={() => setMode('library')} type="button">Bible Library</button><button className={mode === 'manual' ? 'rounded-full bg-red-800 px-4 py-2 text-sm font-bold text-white' : 'rounded-full px-4 py-2 text-sm font-bold'} onClick={() => setMode('manual')} type="button">Manual Entry</button></div><div className="mt-5 grid gap-4">{mode === 'library' ? <><div className="rounded-[2rem] border border-black/10 p-2 dark:border-white/10"><div className="flex flex-wrap items-center gap-2"><div className="relative"><button aria-expanded={openPicker === 'version'} className={darkMode ? 'inline-flex min-h-11 items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 text-sm font-bold hover:bg-white/15' : 'inline-flex min-h-11 items-center gap-2 rounded-full border border-black/10 bg-[#fffaf0] px-4 text-sm font-bold hover:bg-white'} onClick={() => setOpenPicker(openPicker === 'version' ? null : 'version')} type="button">{selectedVersion?.abbreviation || selectedVersion?.id || 'Version'}<ChevronDown className="size-4 text-red-800 dark:text-red-200" /></button>{openPicker === 'version' ? <div className={'absolute left-0 top-12 w-80 ' + menuClass}><BibleVersionPickerList darkMode={darkMode} mode="single" selectedVersionIds={[versionId]} versions={versions} onToggleVersion={(next) => { setVersionId(next); setOpenPicker(null); }} /></div> : null}</div><ScriptureReferencePickerGroup bookMenuClassName="left-0 top-12 w-[min(80vw,34rem)]" books={books} chapterMenuClassName="right-0 top-12 w-80" chapters={chapters} darkMode={darkMode} openMenu={openPicker === 'version' ? null : openPicker} placement="bottom" selectedBookId={bookId} selectedChapterId={chapterId} onBookChange={(next) => { setBookId(next); setOpenPicker(null); }} onChapterChange={(next) => { setChapterId(next); setOpenPicker(null); }} onOpenMenuChange={setOpenPicker} /></div></div><div className="max-h-52 overflow-y-auto rounded-xl border border-black/10 p-2 dark:border-white/10">{loading ? <p className="flex items-center gap-2 p-3 text-sm text-zinc-600 dark:text-stone-300"><LoaderCircle className="size-4 animate-spin" /> Loading Scripture...</p> : verses.map((verse) => <label className={'flex cursor-pointer gap-3 rounded-lg px-3 py-2 text-sm hover:bg-red-950/5 ' + (selectedNumbers.includes(verse.number) ? 'bg-red-950/[0.04]' : '')} key={verse.number}><input checked={selectedNumbers.includes(verse.number)} onChange={() => toggleVerse(verse.number)} type="checkbox" /><span><strong className="mr-2 text-red-800 dark:text-red-200">{verse.number}</strong>{verse.text}</span></label>)}</div><p className="text-sm text-zinc-600 dark:text-stone-300">{selectedReference || 'Choose verses to preview the saved passage.'}</p></> : <><label className="grid gap-1 text-xs font-bold">Reference<input className={inputClass} onChange={(event) => setManualReference(event.target.value)} placeholder="Psalm 119:105" value={manualReference} /></label><label className="grid gap-1 text-xs font-bold">Version label<input className={inputClass} onChange={(event) => setManualVersion(event.target.value)} placeholder="NIV" value={manualVersion} /></label><label className="grid gap-1 text-xs font-bold">Scripture text<textarea className={inputClass + ' min-h-32 resize-y leading-7'} onChange={(event) => setManualText(event.target.value)} placeholder="Your word is a lamp to my feet..." value={manualText} /></label></>}<fieldset className="grid gap-2"><legend className="text-sm font-bold">How should this appear?</legend><div className="grid gap-2 sm:grid-cols-3">{([{ value: 'inline', label: 'Reference only' }, { value: 'block', label: 'Scripture block' }, { value: 'passage', label: 'Scripture passage' }] as const).map((item) => <label className={'cursor-pointer rounded-xl border p-3 text-sm ' + (display === item.value ? 'border-red-800 bg-red-950/5' : 'border-black/10 dark:border-white/10')} key={item.value}><input checked={display === item.value} className="sr-only" name="scripture-display" onChange={() => setDisplay(item.value)} type="radio" value={item.value} />{item.label}</label>)}</div></fieldset>{error ? <p className="text-sm text-red-800 dark:text-red-200">{error}</p> : null}<footer className="flex justify-end gap-3 border-t border-black/10 pt-4 dark:border-white/10"><button className="rounded-full px-4 py-2 text-sm font-bold" onClick={onClose} type="button">Cancel</button><button className="inline-flex items-center gap-2 rounded-full bg-red-800 px-5 py-2 text-sm font-bold text-white" disabled={loading} onClick={insert} type="button"><ScrollText size={16} /> Insert Scripture</button></footer></div></section></div>;
};
export default ScriptureInsertDialog;


